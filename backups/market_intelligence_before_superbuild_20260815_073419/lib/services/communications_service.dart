class CommunicationsService {
  Future<void> sendSms(String to, String message) async {
    throw UnimplementedError('Connect this to a secure backend/Twilio function; never expose private credentials in Flutter.');
  }
  Future<void> sendEmail(String to, String subject, String body) async {
    throw UnimplementedError('Connect this to a secure backend email provider function.');
  }
}
